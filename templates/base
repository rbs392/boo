{{#suites}}
describe('{{desc}}', function({{done}}){
  {{#scenarios}}
  it('{{shud}}', function({{done}}){
    {{#extracts}}
      {{^eval}}
      expect('{{selector}}').to.equal('{{value}}');
      {{/eval}}
      {{eval}}
    {{/extracts}}
  })
  {{/scenarios}}
})
{{/suites}}