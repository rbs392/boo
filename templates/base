/* Test Generated via Boo. :-o*/

const expect = require('chai').expect;
{{#each suites}}
describe('{{desc}}', function({{done}}) {
  {{#each scenarios}}
    it('{{shud}}', function({{done}}) {
        browser.url('{{../../url}}');
      {{#each extracts}}
        {{#extract}}{{/extract}}
      {{/each}}
    });
  {{/each}}
})
{{/each}}